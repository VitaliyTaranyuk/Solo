{
  "name": "repcoach",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "test":       "vitest run --reporter=verbose",
    "test:watch": "vitest --reporter=verbose"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
